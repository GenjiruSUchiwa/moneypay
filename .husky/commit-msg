#!/bin/sh
# husky.sh is generated by `husky install` and is not committed; guard the
# source so the hooks also work in worktrees and fresh clones.
if [ -f "$(dirname "$0")/_/husky.sh" ]; then
  . "$(dirname "$0")/_/husky.sh"
fi

dotnet husky run --name commit-message-linter --args "$1"
