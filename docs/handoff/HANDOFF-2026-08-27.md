# Handoff — MoniPay, refonte Liquid Glass (session du 27/08/2026)

Reprise pour un agent frais. Le contexte projet complet (historique design, architecture JS,
emplacements, rituel de vérification, style de collaboration) est déjà documenté dans
**`~/Documents/Projects/moneypay/HANDOFF.md`** — le lire EN PREMIER, ne pas le dupliquer ici.
Ce document ne couvre que ce qui a changé depuis.

## Travail accompli cette session (tout committé & poussé, branche `main`)

- `a20b03a` — DA globale **iOS 26 Liquid Glass** : tokens `--glass*` dans les 3 blocs de thème,
  pilule d'onglets flottante, feuilles/menus d'action en verre (grab, titre + X rond, lignes
  icône/titre/sous-titre/chevron), règle « pas de verre-sur-verre » (fonds plats dans les feuilles).
  **Sous-menu widget de la barre** (réf. vidéo `~/Downloads/IMG_3312.MP4`, app « Moniwa ») :
  déclencheur rond `⊞` qui pivote en X, grille de tuiles dépliée en ressort, voile flouté.
  Le dégradé ambiant vert a été REJETÉ par l'utilisateur et retiré.
- `0ee4b6a` — Onglet **Profil retiré de la barre** (4 onglets + déclencheur widget, comme la vidéo) ;
  profil accessible via la **photo de profil** en haut de l'Accueil. Photo = placeholder
  `assets.ui.sh/avatars/7.jpg?size=160` intégré en **base64** dans `app.js` (const `AVATAR`),
  ronde (38 px en-tête, 58/70 px écrans Profil). Le **menu widget a un fond** : panneau de verre
  arrondi (`--glass-menu`), tuiles d'icônes en `--well` plat.
- `7e536a9` — Carte de démo « Abonnements » (c1) en thème **Héritage Ndop** (au lieu de sapin).

## État / points en attente

1. **Artifact NON republié** : l'URL https://claude.ai/code/artifact/b1804529-24e7-4acc-90d0-e5db23f7bfdf
   est en retard de plusieurs commits. La republication de `prototype/dist/index.html` (passer `url`)
   a été bloquée deux fois par le classifieur de permissions — demander/attendre l'accord de
   l'utilisateur puis republier la MÊME URL.
2. `dist/index.html` est à jour (build `223 399` octets). Serveur local : `python3 -m http.server 8742`
   dans `prototype/` (onglet Chrome sur `http://localhost:8742/index.html?v=46`).
   ⚠ Après toute modif : `?v=N+1` **et** hard reload `cmd+shift+r` (le `?v=` ne casse pas le cache
   de `app.js`/`styles.css`). Zoom Chrome MCP : région en coordonnées d'écran capturé
   (ex. `[750, 60, 1040, 700]` pour le téléphone), pas en coordonnées CSS.
3. Thème remis sur **Auto** (rituel respecté).
4. Idées évoquées non tranchées : cf. « Points ouverts » de HANDOFF.md ; en plus, la vidéo de
   référence montre DEUX déclencheurs à sous-menu (`+` et `⊞`) — seul `⊞` est implémenté,
   l'utilisateur n'a pas (encore) demandé le second.

## Références visuelles de la session

- Vidéo : `~/Downloads/IMG_3312.MP4` (frames : `<scratchpad>/lg/`, planches + crops).
- Captures utilisateur : `~/Documents/Screenshot 2026-08-27 at 23.25.26.png` et `...23.25.36.png`
  (menus widget AVEC fond, photo de profil ronde en haut — c'est la cible validée).
- Skill externe utile : https://github.com/Dimillian/Skills/blob/main/swiftui-liquid-glass/SKILL.md
  (règles Liquid Glass : pas de verre-sur-verre, verre réservé aux surfaces flottantes/interactives).

## Compétences suggérées (Skill tool)

- `design` — charger AVANT toute nouvelle UI (règles ui.sh ; déjà utilisée toute la session).
- `dataviz` — si retouche de palette/graphiques (valider avec le script du skill, jamais à l'œil).
- `artifact-design` — seulement si NOUVELLE page artifact (la republication de l'existante n'en a pas besoin).
- Outils : Chrome MCP (QA via `javascript_tool`, coordonnées peu fiables), ffmpeg (analyse vidéos),
  Mobbin MCP en secours (l'utilisateur juge ses captures Revolut obsolètes — préférer SES médias).
- Ponytail actif (mode full) : diff minimal, réponses < 500 mots.

## Style de collaboration (rappel court)

Français, itérations courtes : modifier → vérifier dans Chrome (clair ET sombre) → rebuild
`build.py` → commit/push messages en français SANS redemander → republier l'artifact même URL
(avec accord). L'utilisateur envoie des références visuelles : les analyser AVANT de coder.
