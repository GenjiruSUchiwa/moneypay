# xunit.v3 runs on Microsoft.Testing.Platform. Without this, "dotnet test" hands the
# assembly to VSTest, which discovers no tests at all and still reports success.
[dotnet.test.runner]
name = "Microsoft.Testing.Platform"
