namespace MoniPay.Sessions.Features.Sessions;

/// <summary>The OpenAPI tag the session endpoints group under.</summary>
internal static class SessionTags
{
    public const string Sessions = nameof(Sessions);
}
