namespace MoniPay.Sessions;

/// <summary>
/// The lookup key for this module's translated strings. It carries no members: an
/// <c>IStringLocalizer&lt;SessionMessages&gt;</c> resolves
/// <c>Resources/SessionMessages[.&lt;culture&gt;].resx</c> from this type's name and namespace,
/// so the module owns its copy the same way it owns its domain.
/// </summary>
public sealed class SessionMessages;
