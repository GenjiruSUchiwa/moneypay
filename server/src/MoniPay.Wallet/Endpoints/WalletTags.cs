namespace MoniPay.Wallet.Endpoints;

/// <summary>
/// The OpenAPI tags this module groups its endpoints under. One tag per module keeps the
/// generated document and the generated client organized the way the code is.
/// </summary>
public static class WalletTags
{
    public const string Wallet = nameof(Wallet);
}
