namespace MoniPay.Wallet;

/// <summary>
/// The lookup key for this module's translated strings. It carries no members: an
/// <c>IStringLocalizer&lt;WalletMessages&gt;</c> resolves
/// <c>Resources/WalletMessages[.&lt;culture&gt;].resx</c> from this type's name and namespace,
/// so the module owns its copy the same way it owns its endpoints.
/// </summary>
public sealed class WalletMessages;
